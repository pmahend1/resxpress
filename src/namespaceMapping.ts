export class NamespaceMapping{
    
    constructor(public fileName: string,
        public nameSpace: string
    ) {
        this.fileName = fileName;
        this.nameSpace = nameSpace;
        
    }
}